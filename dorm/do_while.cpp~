#include <iostream>
#include <stdlib.h>

using namespace std;



int main(){
	int counter(0),number,max(-2087554);
	do{
		cin>>number;
		if(number!=-1){
			counter++;
		  	if(number>max)
				max=number;
			else
				continue;
	
		}
		else{
			cout<<"'-1' ended this session"<<endl;
			break;
		}
		}while(number!=-1);
	cout<<"Max: "<<max<<endl;
}
