#include <iostream>
#include <string.h>
using namespace std;

struct stud{
	string name;
	fload mark;
	stud *link;
};

stud *insert(stud *head, string name, float mark);

int main(){
	stud *head = NULL;
}
